# URL of the NestJS GraphQL API.
# Must be prefixed with NEXT_PUBLIC_ so it is available in the browser, where
# React Query performs the fetch.
NEXT_PUBLIC_GRAPHQL_URL="http://localhost:4000/graphql"
