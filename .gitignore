# Dependencies
node_modules/
.pnp
.pnp.js

# Builds
dist/
build/
.next/
out/

# Environment
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Testing
coverage/

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# Research material (upstream reference repos + saved articles, ~115 MB).
# Kept locally for context, intentionally not part of the deliverable.
docs/

# NOTE: backend/schema.gql is deliberately NOT ignored. It is generated by the
# code-first GraphQL setup, and committing it lets the frontend run codegen
# without a running backend.
